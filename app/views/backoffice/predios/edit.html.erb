<h1><%= "#{t('message.editing')} #{t('activerecord.models.predio.one')}" %> </h1>

<%= render 'form', predio: @predio %>

<%= link_to t('buttons.show'), [ :backoffice, @predio ], class: 'waves-effect waves-light btn' %>