<%= form_for([:backoffice, bolsa]) do |f| %>
  <% if bolsa.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(bolsa.errors.count, "error") %> prohibited this bolsa from being saved:</h2>

      <ul>
      <% bolsa.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :predio_id %>
    <%= f.text_field :predio_id %>
  </div>

  <div class="field">
    <%= f.label :edital_id %>
    <%= f.text_field :edital_id %>
  </div>

  <div class="field">
    <%= f.label :quantidade %>
    <%= f.number_field :quantidade %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
